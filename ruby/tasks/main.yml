- name: install rvm
  shell: 'curl -sSL https://get.rvm.io | bash -s stable'

- name: install ruby via rvm
  command: bash -lc 'rvm install {{ ruby.version }}'

- name: create gemset
  command: bash -lc 'rvm {{ ruby.version }}@{{ ruby.gemset_name }} --create --default'

